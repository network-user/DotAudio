.pragma library

// Поверхности и чернила. Монохром DotCore: акцент даёт форма и белая
// прозрачность, а не цвет. Единственный цветной сигнал - запись.
var bg = "#0a0b0d"
var surface = "#14161a"
var surface2 = "#1b1d21"
var surface3 = "#21242a"
var text = "#f3f3f1"
var muted = "#a6a7ab"
var faint = "#74757a"
var border = "#22ffffff"
var borderHi = "#33ffffff"
var hairline = "#16ffffff"
var fill = "#14ffffff"
var fillHi = "#20ffffff"
var fillPress = "#2effffff"
var specular = "#26ffffff"
var scrim = "#99000000"
var ink = "#f3f3f1"
var islandFill = "#f214161a"
var overlayFill = "#f20a0b0d"
var overlayFillHigh = "#ff0a0b0d"
var recRing = "#66ffffff"
var rec = "#ff5f57"
var recSoft = "#33ff5f57"

var fontFamily = "Segoe UI"
var monoFamily = "Consolas"

// Размеры текста. Живой субтитр крупнее всего остального: в Live читают
// одну фразу, а не интерфейс вокруг неё. Ступеней ровно столько, сколько
// ролей текста в приложении; произвольные кегли в компонентах не заводить.
var fsMicro = 10    // служебные метки, таймкоды караоке
var fsSmall = 11    // подписи под элементами и пояснения в карточках
var fsLabel = 12    // текст элементов управления и вторичные строки
var fsBody = 13     // основной текст
var fsCompact = 15  // субтитр на острове
var fsTitle = 16    // заголовок карточки
var fsSection = 18  // логотип оболочки и заголовок панели
var fsLead = 21     // заголовок пустого состояния
var fsHero = 24     // крупная строка диктовки
var fsHead = 26     // заголовок страницы
var fsStageSm = 30  // фраза на сцене внутри окна приложения
var fsStage = 34    // фраза на отдельной сцене Live и в зале

// Отступы. Карточка держит одно внутреннее поле, вертикальный ритм страниц
// кратен базовому шагу.
var gapXs = 4
var gapSm = 8
var gapMd = 12
var gapLg = 18
var gapXl = 24
var padCard = 18

var radiusSm = 10
var radiusMd = 14
var radiusLg = 20
var radiusXl = 26

// Длительности. Короткие для отклика на курсор, средние для смены
// содержимого, длинная только для морфа острова. Это проектные значения
// интерфейса, а не измеренная задержка распознавания.
var instantMs = 90
var fastMs = 140
var baseMs = 210
var slowMs = 320
var morphMs = 380
var wordMs = 140
var reflowMs = 300
var promoteMs = 420
var contentMs = 170
var captionWordLimit = 192
// Появление нового слова: короткое всплытие с торможением. Длительность
// намеренно небольшая - слово должно успевать встать в ритм живой речи,
// а не выплывать как заставка.
var wordMs = 130
var wordRise = 14
// Подтверждение крупнее этой волны слов идёт по одному слову, мелкое
// подтверждается сразу.
var confirmWaveWords = 3

// Кривые. easeOut - мягкое торможение без отката, для появления текста.
// easeSpring - лёгкий перелёт для морфа острова и нажатий.
var easeOut = [0.16, 1.0, 0.3, 1.0, 1, 1]
var easeInOut = [0.65, 0.0, 0.35, 1.0, 1, 1]
var easeSpring = [0.34, 1.32, 0.64, 1.0, 1, 1]
var easeExit = [0.4, 0.0, 1.0, 1.0, 1, 1]

// Прозрачность живого хвоста фразы: подтверждённые слова видны полностью,
// ещё уточняемые - приглушены. Начертание одинаковое, чтобы подтверждение
// не меняло ширину слов и переносы строк. Хвост приглушён сильнее, чем
// прежде: недописанное слово должно читаться как черновик, а не как
// застывший текст, и не спорить за взгляд с уже подтверждённым.
var pendingAlpha = 0.56
var historyAlpha = 0.40
// Подтверждение «въезжает» коротким сдвигом снизу и ростом непрозрачности,
// а не мгновенной сменой: взгляд ловит, какие слова дозрели на этом шаге.
var confirmRise = 6

// Шаг строки субтитра. Должен быть выше высоты глифов: сцена показывает
// последние строки и обрезает верхние, а при меньшем шаге снизу оставался
// срез предыдущей строки.
var captionLineFactor = 1.38

// Уровень входа. Усиление и корень нужны только чтобы тихая речь была
// видна на 12-20 px; само значение остаётся измеренным RMS из capture.
var levelGain = 28
var levelCurve = 0.7

function levelShape(level) {
    var raw = Math.max(0, Math.min(1, Number(level) * levelGain))
    return Math.pow(raw, levelCurve)
}
