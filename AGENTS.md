# DotAudio: инструкции агентам

## Контекст

Настольное приложение к статье на Хабре о Whisper.
Последнее указание пользователя в исходной сессии - подготовить основу
для передачи другой модели. Код не считается готовым продуктом.
Перед продолжением прочитай `docs/HANDOFF.md` и `docs/PRODUCT.md`.

## Профиль проекта

- Тип: desktop-app с Python core и необязательным HTTP-сервисом.
- Аудитория: пока internal; будущая демонстрация в статье.
- Дистрибуция: local-only, установщик ещё не реализован.
- Первая платформа: Windows. Остальные платформы не проверены.
- Runtime: Python >=3.12,<3.14, Qt Quick через PySide6.
- `pyproject.toml` - источник правды по зависимостям и entrypoints.
- Git-репозиторий и remote на момент создания не инициализированы.

## Запуск и проверки

Все команды выполнять из корня DotAudio в собственной `.venv`.
Не менять окружения соседних проектов.

```powershell
python -m venv .venv
.\.venv\Scripts\python.exe -m pip install -e '.[dev,server]'
.\.venv\Scripts\dotaudio.exe
.\.venv\Scripts\python.exe -m pytest -q
.\.venv\Scripts\python.exe -m ruff check src tests
```

Без editable-установки использовать `PYTHONPATH=src` и `python -m dotaudio`.
Для QML smoke-test задать `QT_QPA_PLATFORM=offscreen` и передать
`--smoke-test --data-dir .local-check`.
Не скачивать модели и не включать микрофон в обычных unit-тестах.
Сборка установщика ещё не настроена; не выдумывать команду build.

## Структура

```text
src/dotaudio/
  app.py, controller.py, desktop.py  Qt и Windows
  qml/MainMvp.qml, MiniIsland.qml, LiveTheater.qml, CaptionOverlay.qml
                                      оболочка, остров, Live-сцена, субтитры зала
  qml/Theme.js, Icon.qml, Waveform.qml, CaptionText.qml, TranscriptEditor.qml, KaraokePreview.qml
                                      токены, глифы, waveform, редактор и karaoke
  capture.py, pipeline.py           захват и live-очередь
  engine.py                        локальный/удалённый ASR
  storage.py, transcripts.py        история и текстовый экспорт
  karaoke.py                        ASS и MP4-экспорт караоке
  server.py                        optional FastAPI
tests/
deploy/
docs/
```

## Принципы реализации

- Не помещать inference, скачивание модели и декодирование в GUI thread.
- Не обращаться к QML-объектам из worker threads; использовать Qt signals.
- Сохранять Qt-free границу engine/capture/storage/transcripts.
- Таймкоды сегментов: конечные секунды, start >= 0, end >= start.
- Различать stop с сохранением хвоста и cancel без автовставки.
- Не допускать повторного завершения сессии и потери аудио без статуса.
- Ограничивать очереди и кеш модели, измерять рост памяти.
- Не отправлять Enter после автовставки. Проверять целевое окно.
- Не подменять фактические уровни аудио и результаты демонстрационными.
- Поддержку устройства/ОС и скорость заявлять после реальной проверки.
- Конфигурация серверного режима не означает готовность public API.

## Соглашения

- Ответы, интерфейс и документация на русском, если не запрошено иное.
- Имена Python-модулей и API на английском; следовать Ruff из pyproject.
- Сначала читать затронутый код и соседние тесты, затем менять.
- Тестировать поведение и границы, не писать тесты ради числа проверок.
- Сохранять исходный текст расшифровки при правках.
- Для завершённых правок предлагать Conventional Commit на английском.
- Не добавлять Co-Authored-By и строки о генерации ИИ.

## Пользовательские ограничения

- Не удалять файлы без явного подтверждения пользователя.
- Не выполнять commit/push/reset --hard/force push без явного запроса.
- Не читать и не выводить `.env*`, `*.pem`, `*.key`, `*.p12`, `*.pfx`,
  `secrets.json`, `credentials.json`, `service-account*.json` и файлы,
  в имени или пути которых есть secret/token/credential/password/private.
- Секретное значение при необходимости запрашивать отдельно у пользователя.
- Соседние DotSoundBackend и DotSoundComputeWorker изучать только чтением.
- Не копировать закрытые политики .звук в самостоятельный проект.
- Для независимого изучения DotSound пользователь предпочёл Terra-агентов.
- Не менять LICENSE без явного запроса; условия для читателей статьи открыты.

## Документация и DotCore

- `docs/HANDOFF.md` - статус, проверки, ближайшие задачи и запрос следующей модели.
- `docs/PRODUCT.md` - требования и предложения; не всё уже реализовано.
- `docs/ARCHITECTURE.md` - контракты, границы и технические решения.
- `docs/RESEARCH.md` - источники и выводы по .звук.
- `README.md` - фактический запуск и краткий состав.
- README-sync: при изменении команд, модулей, зависимостей или архитектуры
  обновлять README через `generate-readme`, правила через `sync-project-rules`.
- Правила дополнять, не переписывать авторские блоки.
- Не удалять loc-маркеры и не придумывать число строк, бенчмарки или проверки.
- SVG-обложка README монохромная; основной UI ещё требует дизайна Apple + DotCore.
